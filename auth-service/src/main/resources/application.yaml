spring:
  application:
    name: auth-service
  datasource:
    url: 
    username: 
    password: 

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081

logging:
  level:
    org.springframework.web: DEBUG
    com.company.auth: DEBUG
